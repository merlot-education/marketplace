import {NumericDirective} from './numeric.directive';

describe('NumericDirective', () => {
  it('should create an instance', () => {
    const directive = NumericDirective;
    expect(directive).toBeTruthy();
  });
});
